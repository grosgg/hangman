require 'spec_helper'

describe Game do
    it "has a session attached"
    it "has a round total"
    it "has a current round"
    it "has a current word"
    it "has a category"
    it "has a language"

    it "checks how many wrong letters were used"
    it "checks if a letter was already used"
    it "checks if all correct letters were found"
end
